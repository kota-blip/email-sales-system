# 請求書集計システム（払う側）

Gmailに届く請求書・見積書・納品書のPDFを自動検出し、Claudeで内容を読み取り、
Googleスプレッドシートに記録・月次集計するシステムです。
「絶対に漏れない」を実現するため、以下の二段構えで漏れを検知します。

1. **固定支払い先リストとの突合**：毎月来るはずの取引先（家賃・サブスク等）が今月来ているかチェック
2. **月末の人間による最終目視チェック**：自動集計結果をTelegramで確認し、確定ボタンを押すまで月次は「未確認」のまま

さらに、見積書・納品書・請求書の **3点セットが揃っているか** も案件単位でチェックします。

新規書類を検出するたびに **「これは支払うものですか？」の支払対象アラート** も出します。
自社が支払う側だと確信できれば「💰支払うものです」と明示し、判断できなければボタンで確認を求めます
（詳細は後述の「支払対象アラートの仕様」）。

支払い（振込）は一切行いません。集計・記録・ダブルチェックまでがこのシステムの範囲です。

---

## アーキテクチャ

```
Gmail（請求書PDF添付） ──┐
                         ├─▶ invoice_system_main.py ──▶ Google Sheets（真のデータソース）
Claude API（読み取り） ───┘         │
                                   ▼
                          Telegram（通知・承認/修正/却下・月末確定）
```

- `gmail_handler.py` … Gmail検索・PDF添付取得（既存モジュールに追加）
- `invoice_extractor.py` … PDFテキスト抽出 + Claudeによる構造化データ抽出
- `invoice_matcher.py` … 3点セット紐付け（ヒューリスティック）＋固定支払い先の漏れ検知
- `sheets_client.py` … Googleスプレッドシートの読み書き
- `telegram_handler.py` … Telegram通知・承認ボタン
- `invoice_system_main.py` … 上記を統合するメインアプリ（Flask + バックグラウンド監視）

既存の営業メール返信システム（`email_system_main.py` 等）とは完全に独立して動作します。

---

## セットアップ手順

### 1. Google Cloudサービスアカウントの作成（Sheets書き込み用）

1. https://console.cloud.google.com/ にアクセスし、新規プロジェクトを作成（例: `invoice-aggregation`）
2. 左メニュー「APIとサービス」→「ライブラリ」で以下を検索して有効化する
   - **Google Sheets API**
   - **Google Drive API**（新規スプレッドシート作成・共有に必要）
3. 「APIとサービス」→「認証情報」→「認証情報を作成」→「サービスアカウント」
   - 名前は任意（例: `invoice-bot`）。ロールは付与不要でOK
4. 作成したサービスアカウントの「キー」タブ→「鍵を追加」→「新しい鍵を作成」→ **JSON** を選択してダウンロード
5. ダウンロードしたJSONファイルをリポジトリ直下に `service_account.json` として配置
   （**`.gitignore` 済みなのでコミットされません**。Heroku等にデプロイする場合は環境変数や config vars 経由でファイル内容を配置してください）
6. `.env` の `GOOGLE_SERVICE_ACCOUNT_FILE` にファイルパスを設定（デフォルトは `service_account.json`）

> 既存のスプレッドシートを使いたい場合は、そのシートをサービスアカウントのメールアドレス
> （JSON内の `client_email`、`xxx@xxx.iam.gserviceaccount.com` の形式）に **編集者として共有** し、
> シートIDを `.env` の `GOOGLE_SPREADSHEET_ID` に設定してください。
>
> 未設定のまま初回起動すると、システムが自動で新規スプレッドシートを作成し、
> `.env` の `GOOGLE_SHEET_SHARE_EMAIL` に指定したGoogleアカウントに共有します。
> 起動ログに表示されるスプレッドシートIDを、以後は `.env` の `GOOGLE_SPREADSHEET_ID` に設定してください
> （再作成を防ぐため）。

### 2. Telegram Botの準備

既にBotトークンをお持ちとのことなので、以下だけ確認してください。

1. `.env` の `TELEGRAM_BOT_TOKEN` にトークンを設定
2. 自分の `chat_id` を取得する（Botに何かメッセージを送った状態で以下を実行）
   ```bash
   curl "https://api.telegram.org/bot<TOKEN>/getUpdates"
   ```
   レスポンス中の `"chat":{"id": ...}` の値が `chat_id` です。`.env` の `TELEGRAM_CHAT_ID` に設定してください。
3. Webhookを設定する（デプロイ後、公開URLが決まってから実行）
   ```bash
   curl "https://api.telegram.org/bot<TOKEN>/setWebhook?url=https://<あなたのデプロイ先ドメイン>/telegram_webhook"
   ```

### 3. Gmail認証

既存の `credentials.json` / `token.pickle`（メール返信システムと共通のGmail認証）をそのまま使います。
未設定の場合は既存READMEの手順（`credentials.json` を用意し初回起動時にブラウザ認証）に従ってください。
スコープは `gmail.modify` のままで請求書検索にも対応しています。

### 4. .env の作成

`.env.example` をコピーして `.env` を作成し、値を埋めてください。

```bash
cp .env.example .env
```

### 5. 依存関係のインストール

```bash
pip install -r requirements.txt
```

### 6. 固定支払い先マスタの初期投入

初回起動でスプレッドシートに「固定支払い先マスタ」タブが自動作成されます。
既にお持ちのExcel/スプレッドシート管理リストから、以下の列に転記してください（1行目はヘッダー）。

| 取引先名 | 頻度 | 概算金額 | 備考 | 有効 |
|---|---|---|---|---|
| 例：〇〇オフィス（家賃） | 毎月 | 300000 | 月末締め | TRUE |
| 例：△△クラウドサービス | 毎月 | 5000 |  | TRUE |

「有効」列を `FALSE` にすると、その取引先は漏れ検知の対象から外れます（解約時など）。

### 7. 起動

```bash
python invoice_system_main.py
```

起動すると以下が自動で動きます。

- Gmail監視ループ（`INVOICE_CHECK_INTERVAL` 秒ごと、デフォルト1時間）
- 月次スケジュールループ（`REPORT_AUTO_TRIGGER_DAY` 日ごろに前月分の集計を自動生成しTelegramへ通知、
  `CONFIRMATION_REMINDER_DAY` 日ごろに未確認ならリマインド、`CONFIRMATION_DEADLINE_DAY` 日ごろに未確認なら期限アラート）

手動でGmailスキャンや集計を行いたい場合は、Telegramで以下を送信します。

- 「スキャン」… 今すぐGmailを確認
- 「一覧」… 未承認/要確認の書類一覧
- 「集計 2026-07」… 指定月の集計を実行（省略時は先月分）

### 8. Heroku等へのデプロイ

`Procfile` に `invoice_web` / `invoice_worker` プロセスを追加済みです。
Webhookを使う場合は `invoice_web`（gunicorn）を、シンプルに1プロセスで完結させる場合は
`invoice_worker`（Flask開発サーバー込みで監視ループとWebhookを両方持つ）のいずれかを有効化してください。
サービスアカウントJSONは `.gitignore` 対象のため、Herokuでは config vars 経由でファイル内容を書き出す
仕組み（起動時に環境変数からファイルを生成する等）を別途用意してください。

---

## ダブルチェックの流れ

1. Gmailで請求書/見積書/納品書のPDFを検出
2. Claudeが内容を読み取り、Googleスプレッドシート「請求書ログ」に記録（ステータス：未承認）
3. Telegramに抽出結果が通知される（✅承認 / ✏️修正 / ❌却下）
4. 「✏️ 修正」を押すと、修正内容をテキストで送信するよう案内される。指示を送るとClaudeが再抽出し、再度承認依頼が届く
5. 「✅ 承認」で確定（ステータス：承認済み、承認者・承認日時を記録）

これが「AIが抽出→人間が1回承認」のダブルチェックです。

## 支払対象アラートの仕様

Gmailの検索条件だけでは「自社が支払う請求書」以外（自社が発行した請求書や無関係な書類）が
混ざる可能性があるため、書類ごとに支払対象かどうかも判定・確認します。

- `.env` の `OWN_COMPANY_NAME` に自社名を設定すると、Claudeが「請求先/宛先」と「発行元」を
  比較し、以下のいずれかを判定します。
  - **要払い**：請求先が自社名と一致 → 通知に「💰 支払うものです」と明示（宣言型アラート）
  - **対象外**：発行元が自社名と一致（＝自社が発行した請求書等） → 「🚫 対象外の可能性」と明示
  - **確認中**：`OWN_COMPANY_NAME` 未設定、または判別不能 → 「❓ これは支払うものですか？」と
    ボタン（💰支払う／🚫対象外）で人間に確認を求める（質問型アラート）
- 人間はいつでもボタン、または「修正」コマンドの自然文（例：「これは対象外です」）で上書き可能
- **「対象外」に確定した書類は月次集計の件数・合計金額から自動的に除外**されます

これが「両サイドから支払うものか確認する」仕様です（システムからの宣言・質問＋人間の確認/上書き）。

## 月次集計・最終確認の流れ

1. `REPORT_AUTO_TRIGGER_DAY`（既定10日）ごろに前月分の自動集計が走る（または「集計 YYYY-MM」で手動実行）
2. スプレッドシート「月次サマリ」に、件数・合計金額・3点セット未完了件数・
   固定支払い先の未着リスト・未承認件数を記録
3. Telegramにサマリが通知され、「✅ 今月分を確認済みにする」ボタンが表示される
4. `CONFIRMATION_REMINDER_DAY`（既定15日）ごろ：まだ未確認なら軽いリマインドを送る
5. `CONFIRMATION_DEADLINE_DAY`（既定20日）ごろ：まだ未確認なら期限アラート（🚨）を送る
6. 人間が内容を目視確認し、ボタンを押すと「月次サマリ」の「最終確認」列が確定される

ボタンを押すまで月は「未確認」のままなので、確認漏れが可視化されます。
（締め＝暦月の1日〜末日、自動集計＝翌月10日ごろ、人間の最終確認＝翌月15〜20日ごろ、という運用を想定）

---

## 既知の制限事項（v1）

- **3点セットの紐付けは完全一致IDではなくヒューリスティック**（取引先名の類似度＋金額の近さ＋時期の近さ）です。
  誤マッチ・未マッチの可能性があるため、最終的な整合性は月次サマリと目視チェックで担保してください。
- **画像スキャンのみのPDF（OCR未対応）は自動読み取りできません**。「要確認」として通知されるので、手動で内容を確認・修正してください。
- **重複請求書の自動検知は今回のスコープ外**です（要望に応じて追加可能）。
- **支払対象アラート（要払い/対象外の判定）もヒューリスティック**です。`OWN_COMPANY_NAME` 未設定時や
  書類に請求先/発行元の記載が薄い場合は「確認中」となり、人間の確認待ちになります。
- Telegramの承認状態は **プロセス内メモリ** で一部管理しているため（修正待ち状態など）、
  デプロイ時は Web dyno を1プロセス（`-w 1`）で運用してください。
